# Simplify engine bindings. Plan, Build, and Review own their defaults in SKILL.md.
# Dev calls those skills sequentially.
pipelines:
  simplify:
    simplify-reuse: [grok]
    simplify-simplification: [grok]
    simplify-efficiency: [grok]
    simplify-altitude: [grok]
