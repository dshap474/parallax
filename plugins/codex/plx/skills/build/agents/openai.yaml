interface:
  display_name: "PLX::Build"
  short_description: "One build worker implements and verifies an accepted spec"
  default_prompt: "Use $plx:build to hand this accepted spec to one build worker that implements it and runs the full relevant verification suite."
policy:
  allow_implicit_invocation: false
