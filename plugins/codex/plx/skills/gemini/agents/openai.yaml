interface:
  display_name: "PLX::Gemini"
  short_description: "Run one Gemini CLI passthrough"
  default_prompt: "Use $plx:gemini to send this request to Gemini CLI."
policy:
  allow_implicit_invocation: false
