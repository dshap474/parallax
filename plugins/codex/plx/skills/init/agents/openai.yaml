interface:
  display_name: "PLX::Init"
  short_description: "Prime the orchestrator's delegation posture"
  default_prompt: "Use $plx:init to prime this session's Parallax delegation posture."
policy:
  allow_implicit_invocation: false
