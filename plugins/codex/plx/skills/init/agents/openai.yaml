interface:
  display_name: "PLX::Init"
  short_description: "Load the Parallax skill map and defaults"
  default_prompt: "Use $plx:init to load the Parallax skill map and model defaults into context."
policy:
  allow_implicit_invocation: false
