interface:
  display_name: "PLX::Init"
  short_description: "Bootstrap repository agent documentation"
  default_prompt: "Use $plx-init to bootstrap this repository's agent documentation."
policy:
  allow_implicit_invocation: false
