interface:
  display_name: "PLX::KISS"
  short_description: "Load the user's KISS principles into context"
  default_prompt: "Use $plx:kiss to load my KISS principles into this task's context."
policy:
  allow_implicit_invocation: false
