interface:
  display_name: "PLX::Review"
  short_description: "Run three Grok review lanes and fix findings"
  default_prompt: "Use $plx:review to review my current changes and fix confirmed findings."
policy:
  allow_implicit_invocation: false
