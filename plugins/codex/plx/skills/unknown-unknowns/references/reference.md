# Reference extraction

Read the supplied code, site, document, or diagram; ask for a pointer if missing.
Inspect underlying code when useful. Extract desired behavior, structure, and edge
cases into a short contract the user can confirm.
