# /plx:dev — full pipeline: plan -> build (worker spawns 2 reviewers) -> gate -> docs -> local commit
NEEDS: codex
FIXTURE: calc
TASK: Add a median(numbers) function to calc.py that returns the middle value (the mean of the two middle values for even-length input), plus a matching test in test_calc.py. Keep it minimal.
EXPECT_DIFF: def median
EXPECT_CHECK: python3 -c "from calc import median; assert median([1,2,3])==2 and median([1,2,3,4])==2.5"
EXPECT_OUTPUT: review
