# /plx:gemini — sandboxed file edit; authentication failures must fail the check
FIXTURE: calc
TASK: Add an empty-list guard to average() in calc.py so average([]) returns 0.0 instead of raising ZeroDivisionError. Edit calc.py only; keep it minimal. Do not execute shell commands or tests in Gemini.
EXPECT_DIFF: average
EXPECT_CHECK: python3 -c "from calc import average; assert average([])==0.0 and average([1,2,3])==2"
