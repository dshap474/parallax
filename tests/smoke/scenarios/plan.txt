# /plx:plan — author + red-team a plan; no code written (plan is in-context for a small task)
NEEDS: codex
FIXTURE: calc
TASK: Plan how to add a median(numbers) function to calc.py (middle value; mean of the two middle values for even-length input) with a matching test in test_calc.py. Keep it minimal.
EXPECT_EMPTY_DIFF: true
EXPECT_OUTPUT: median
